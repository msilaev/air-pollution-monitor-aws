# Streamlit dashboard Dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
ENV PYTHONPATH=/app
EXPOSE 8501
CMD ["streamlit", "run", "src/frontend/dashboard_simplified.py", "--server.port=8501", "--server.address=0.0.0.0"]
